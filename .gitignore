# 依赖与缓存
__pycache__/
*.py[cod]
.venv/
venv/

# 密钥与本地配置
.env

# 运行产物（每次运行重新生成）
outputs/*
# 例外：报告配图（AI 生成、消耗积分）随仓库分发，确保克隆后开箱即用带图
!outputs/images/
!outputs/images/*.png
*.jsonl
eval/report.json
eval/last_run_summary.txt

# 编辑器
.idea/
.vscode/
